{
  "name": "privatechat-voice",
  "version": "0.1.0",
  "private": true,
  "description": "PrivateChat voice server — MediaSoup + Socket.io",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/src/server.js",
    "start:dev": "tsc -w -p tsconfig.build.json"
  },
  "dependencies": {
    "ioredis": "^5.7.0",
    "mediasoup": "^3.15.5",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
